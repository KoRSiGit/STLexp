# ООО «Сибтехлит» — Официальный веб-сайт и ИИ-Ассистент

Современный, быстрый и отзывчивый веб-сайт для компании **«Сибтехлит»** (Сибирские Технологии Литья, г. Новосибирск). Сайт разработан на основе стека **React 19**, **Vite**, **TypeScript**, **Tailwind CSS v4** и включает встроенный модуль интеллектуальных расчетов параметров плавок/смесей, а также полноценного интерактивного **ИИ-Инженера**, использующего современные модели Gemini.

---

## 🛠️ Стек технологий

*   **Frontend:** React 19, TypeScript, Tailwind CSS v4 (с плагином `@tailwindcss/vite`).
*   **Backend:** Node.js, Express (служит для безопасного проксирования запросов к API и раздачи статики).
*   **ИИ-Ассистент:** `@google/genai` (Gemini API).
*   **Анимации:** `motion/react` (плавные интерактивные переходы и анимация табов).
*   **Иконки:** `lucide-react`.

---

## ⚡️ Автономный и Статический режимы (Для GitHub Pages)

Данное приложение поддерживает **комбинированную гибридную архитектуру**. Если бэкенд-сервер недоступен (например, при размещении на статической платформе вроде **GitHub Pages**), все динамические функции плавно переключаются в локальный автономный режим работы:
1. **ИИ-Ассистент (Оффлайн режим):** При блокировке запросов к бэкенду чат с ИИ переключается на встроенный экспертный симулятор металлургических справочных данных. Он реагирует на ключевые литейные темы (ХТС-процесс, смола, регенерация, индукционные печи, плавка) и предлагает корректное инженерное руководство по режимам технологических операций СЛТ.
2. **Отправка спецификаций КП:** При нажатии «Отправить запрос» в корзине оборудования система автоматически создаст уникальную спецификацию с идентификатором `КД-YYMMDD-XXXX-STATIC` в памяти браузера, выведет подтверждение успешной сессии и предложит контакты для быстрого прямого запроса комплекта на `info@sibtehlit.ru`.

---

## 🚀 Пошаговое руководство по сборке и локальному запуску

Для работы проекта требуется установленная среда выполнения **Node.js** (версии 18 или новее).

### 1. Клонирование репозитория и установка зависимостей
Склонируйте репозиторий с GitHub и установите все необходимые пакеты:
```bash
git clone <URL_ВАШЕГО_РЕПОЗИТОРИЯ_НА_GITHUB>
cd sibtehlit-site
npm install
```

### 2. Настройка переменных окружения
Для работы ИИ-инженера (Gemini) вам понадобится API-ключ. Создайте в корне проекта файл `.env` на основе примера `.env.example`:
```bash
cp .env.example .env
```
Откройте файл `.env` и вставьте ваш API-ключ Gemini:
```env
GEMINI_API_KEY=your_gemini_api_key_here
```
*(Получить ключ можно бесплатно в [Google AI Studio](https://aistudio.google.com/))*

### 3. Запуск в режиме разработки
Для мгновенного горячего обновления кода в процессе редактирования (HMR) запустите сервер разработки:
```bash
npm run dev
```
Сайт будет доступен по адресу: `http://localhost:3000`

---

## 📦 Компиляция проекта (Сборка для публикации)

В данном проекте используется архитектура **Full-Stack**. Кнопка сборки компилирует как клиентскую статическую часть (React), так и сервер на Node.js (Express):

```bash
npm run build
```

**Что происходит при сборке (`npm run build`):**
1. `vite build` — Компилирует React-приложение, минифицирует стили, оптимизирует картинки и складывает статические файлы в директорию `dist/`.
2. `esbuild server.ts ...` — Компилирует TypeScript-файл сервера `server.ts` в единый оптимизированный файл `dist/server.cjs`, готовый к запуску.

---

## 🌐 Как развернуть собранный сайт на хостинге

### Вариант А. Деплой на GitHub Pages (Полностью статический SPA-режим)

Если вам нужно разместить сайт бесплатно на GitHub Pages под вашим аккаунтом:

1. Откройте файл `vite.config.ts`.
2. Добавьте свойство `base: '/имя_вашего_репозитория/'` в объект конфигурации (если адрес сайта будет вида `username.github.io/repo-name`). Если подключается кастомный домен, base можно оставить `/`.
   ```typescript
   export default defineConfig(() => {
     return {
       base: '/sibtehlit-site/', // замените на имя Вашего GitHub репозитория
       plugins: [react(), tailwindcss()],
       // ... остальные свойства
   ```
3. Скомпилируйте приложение:
   ```bash
   npm run build
   ```
4. Деплой на GitHub Pages можно сделать с помощью пакета `gh-pages`:
   ```bash
   npm install gh-pages --save-dev
   ```
   Добавьте в раздел `"scripts"` файла `package.json` две строки:
   ```json
   "predeploy": "npm run build",
   "deploy": "gh-pages -d dist"
   ```
5. Опубликуйте в один клик:
   ```bash
   npm run deploy
   ```
   *Все интерактивные расчеты, корзина и ИИ-Ассистент будут работать автономно в клиентском режиме!*

### Вариант Б. Деплой через Docker (с поддержкой full-featured живого ИИ)
В корне проекта создайте файл `Dockerfile` со следующим содержимым:
```dockerfile
FROM node:18-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production
COPY --from=builder /app/dist ./dist
EXPOSE 3000
ENV NODE_ENV=production
CMD ["npm", "run", "start"]
```
Запустите собранный контейнер на любом VPS или облаке (Cloud Run, Amvera).

### Вариант В. Деплой на облачные хостинги (Render, Railway, Heroku)
1. Подключите ваш GitHub репозиторий к платформе (например, Railway или Render).
2. Настройки:
   - **Environment:** `Node`
   - **Build Command:** `npm install && npm run build`
   - **Start Command:** `npm run start`
3. Добавьте переменную окружения `GEMINI_API_KEY` в панели хостинга для живого чата.

---

## 🗄️ Структура важных разделов кода
*   `/src/App.tsx` — Главный координатор приложения (управление вкладками, переключение языков).
*   `/src/components/AboutCompany.tsx` — Раздел «О компании», структура и референс-лист выполненных проектов ТСО «Сибтехлит».
*   `/src/components/ProductCatalog.tsx` — Интерактивный каталог оборудования с выбором подразделов, характеристиками и формой заказа КП.
*   `/src/components/FoundryCalculator.tsx` — Калькулятор шихты ХТС/ПГС и компонентов смесей.
*   `/src/components/AIAssistant.tsx` — Интерактивный чат с ИИ-Инженером-металлургом.
*   `/server.ts` — Проксирующий бэкенд для защиты API-ключей.
